<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :costumer_id %>
    <%= form.text_field :costumer_id, id: :order_costumer_id %>
  </div>

  <div class="field">
    <%= form.label :payment_type %>
    <%= form.text_field :payment_type, id: :order_payment_type %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost, id: :order_cost %>
  </div>

  <div class="field">
    <%= form.label :delivery_info_id %>
    <%= form.text_field :delivery_info_id, id: :order_delivery_info_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
